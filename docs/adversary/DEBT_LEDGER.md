# Adversary debt ledger

Tracked findings a **human** acknowledged instead of fixing in the PR that surfaced them. The adversary keeps reporting these on future PRs that touch the file until fixed.

**Rules:** only a human maintainer may append a row (never the bot). Acknowledging does not delete the finding — it lets the current PR proceed. See `PREEXISTING_DEBT_POLICY.md`.

| ID | File | Anchor | Acknowledged by | PR | Date | Note |
|----|------|--------|-----------------|-----|------|------|
